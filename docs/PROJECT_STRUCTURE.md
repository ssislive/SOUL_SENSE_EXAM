# 📁 Project Structure — SOUL_SENSE_EXAM

This document explains the folder structure of the **SOUL_SENSE_EXAM** repository and the purpose of its key files and directories. The project is organized to promote modularity, maintainability, and ease of collaboration.

---

```text
SOUL_SENSE_EXAM/
│
├── app/                     # Core application package
│   ├── __init__.py
│   ├── main.py              # Tkinter application entry point
│   ├── config.py            # Centralized configuration (DB_URL, paths)
│   ├── db.py                # Database connection & Session factory
│   ├── models.py            # SQLAlchemy declarative models
│   ├── auth.py              # User authentication logic
│   ├── questions.py         # Question loading Logic
│   └── utils.py             # Shared helpers
│
├── migrations/              # Alembic migrations directory
│   ├── versions/            # Migration scripts (e.g., initial_schema)
│   ├── env.py               # Alembic environment configuration
│   └── script.py.mako       # Migration script template
│
├── scripts/                 # Maintenance scripts
│   ├── __init__.py
│   └── load_questions.py    # loads questions.txt into the DB
│
├── data/
│   └── questions.txt        # Source question bank
│
├── db/
│   └── soulsense.db         # SQLite database
│
├── tests/                   # Pytest test suite
│   ├── conftest.py          # Shared test fixtures (temp DB)
│   ├── test_db.py
│   ├── test_models.py
│   ├── test_auth.py
│   └── test_questions.py
│
├── logs/
│   └── soulsense.log        # Application logs
│
├── alembic.ini              # Alembic configuration file
├── pytest.ini               # Pytest configuration
├── requirements.txt         # Project dependencies
└── README.md
```

---

## Directory & File Overview

### `app/`

The main application package containing all core logic.

- **`config.py`** – Holds centralized settings like database URLs and file paths.
- **`db.py`** – Manages the SQLAlchemy engine, session creation, and auto-migrations.
- **`models.py`** – Defines the database schema using SQLAlchemy ORM (User, Score, etc.).
- **`auth.py`** – Handles secure user registration and login using the ORM.

### `migrations/`

Generated by **Alembic** to handle database schema changes over time.

- **`versions/`** – Contains individual migration scripts that apply changes to the database.
- **`env.py`** – Configures how Alembic connects to the database to run migrations.

### `tests/`

Automated test suite using **pytest**.

- **`conftest.py`** – Sets up a temporary in-memory database fixture for isolated testing.
- **`test_*.py`** – Various test modules for different parts of the application.

---
